<div>
	<? if ($creator = \omSocialLogin\UserMeta::getCreatorProviderName($user_id)) { ?>
		<? include __DIR__ . '/../img/' . strtolower($name) . '.svg' ?>
	<? } else { ?>
		<? foreach (\omSocialLogin\UserMeta::getConnectedProviders($user_id) as $name => $meta) { ?>
			<? include __DIR__ . '/../img/' . strtolower($name) . '.svg' ?>
		<? } ?>
	<? } ?>
</div>